# accessibility-checklist.md (stub)

> **[stub]** — This file is referenced from `ui-design-system/SKILL.md` but is not yet implemented in the public release. It should provide a WCAG AA accessibility checklist.

When implementing UI, this file should cover:
- Color contrast (4.5:1 for text, 3:1 for UI elements)
- Keyboard navigation (tab order, focus visible)
- ARIA labels for interactive elements
- Alt text for images
- Form labels + error messages
- Skip links for screen readers
- Reduced-motion preference (`prefers-reduced-motion`)

**To fill this stub**, copy the developer's local Vault version (if present) or implement from scratch.