# scaffold-templates.md (stub)

> **[stub]** — This file is referenced from `product-build-blueprint/SKILL.md` and `tasks/new-product.md` but is not yet implemented in the public release. It should provide template snippets for the scaffold step.

This file should cover:
- Quick-start templates for: Express + PostgreSQL, Next.js + Tailwind, FastAPI + SQLite
- `package.json` / `requirements.txt` minimal viable versions
- Recommended folder structure (`src/`, `tests/`, `docs/`, `scripts/`)
- `.env.example` boilerplate per stack
- README quick-start template

**To fill this stub**, copy the developer's local Vault version (if present) or implement from scratch.

> Note: scaffold scripts (scaffold-node.sh, scaffold-python.sh) already exist in `scripts/`. This file should document the templates that those scripts use.