# common-stacks.md (stub)

> **[stub]** — This file is referenced from `product-build-blueprint/SKILL.md` but is not yet implemented in the public release. It should provide a catalog of common tech stacks with their tradeoffs.

This file should cover:
- Node.js + Express + PostgreSQL (classic CRUD API)
- Next.js + Supabase + Tailwind (modern full-stack web app)
- Python + FastAPI + PostgreSQL (data-heavy backend)
- Node.js + Fastify + Redis (high-throughput API)
- Python + Flask + SQLite (small scripts/bots)

For each: when to use, learning curve, deployment options, typical project size.

**To fill this stub**, copy the developer's local Vault version (if present) or implement from scratch.